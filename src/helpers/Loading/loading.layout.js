import "./loading.css";

const Loading = () => {
    return (
        <div className="loader-container loader_container">
            <div className="spinner spinner_frame"></div>
        </div>
    );
};

export default Loading;
